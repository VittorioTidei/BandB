spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bandb
    driver-class-name:
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      database: mysql
      database-platform: org.hibernate.dialect.MySQLDialect
  mvc:
    view:
      prefix: /
      suffix: .jsp

